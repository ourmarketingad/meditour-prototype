# 작업 규칙 (아워마케팅 공통 표준 v1 — 2026-08-05)

## 작업 전
- PROJECT_STATUS.md를 먼저 읽고 현재 상태를 파악한다.
- 바꾸려는 코드를 참조하는 모든 파일을 찾아 영향 범위를 파악한다.
- 요청 범위를 벗어난 기능 추가·리팩토링을 하지 않는다.

## 작업 후 — 필수 검증
- typecheck / lint / test / build를 실행한다 (프로젝트에 있는 것 전부).
- 웹사이트·웹앱이면 실제 브라우저(Playwright) QA: 반응형(320/390/768/1440 가로스크롤)·콘솔 오류·JS 예외·내부 링크·폼(빈 값·잘못된 형식·중복 클릭·서버 오류)·404·접근성.
- 고친 부분만이 아니라 전체 테스트를 재실행한다 (회귀 방지).
- 테스트 실패 상태로 배포하지 않는다.
- "문제없음" 결론은 테스트 출력(PASS 목록)을 근거로만 말한다.

## 금지·승인 필요
- 프로덕션 배포는 대표 승인 전 금지.
- 유료 API 추가·DB 마이그레이션·외부 메시지 발송·결제 변경은 대표 승인 필요.
- API 키·토큰·개인정보를 코드나 문서에 기록하지 않는다 (환경변수 사용).
- 블로그·콘텐츠 발행을 자동화하지 않는다 — 소재(원고·이미지)만 준비, 발행은 사람이 직접.
- 인물의 경력·이력·소속은 대표 확인 후에만 노출한다.

## 작업 종료 시
- PROJECT_STATUS.md를 갱신한다 (날짜·완료한 것·남은 것).
- git 저장소면 커밋·푸시(ourmarketingad 비공개)까지 한다.
